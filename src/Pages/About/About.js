import React from 'react';
import Loading from '../SharedPages/Loading/Loading';

const About = () => {
    return (
        <div>
            <Loading />
        </div>
    );
};

export default About;